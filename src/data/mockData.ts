import type { Packet, Port } from '../types';

export const mockPorts: Port[] = [
  { number: 22, protocol: 'TCP', serviceName: 'SSH', packetCount: 15, isActive: true },
  { number: 80, protocol: 'TCP', serviceName: 'HTTP', packetCount: 42, isActive: true },
  { number: 443, protocol: 'TCP', serviceName: 'HTTPS', packetCount: 128, isActive: true },
  { number: 3000, protocol: 'TCP', serviceName: 'Dev Server', packetCount: 8, isActive: true },
  { number: 5432, protocol: 'TCP', serviceName: 'PostgreSQL', packetCount: 23, isActive: false },
  { number: 8080, protocol: 'TCP', serviceName: 'HTTP Alt', packetCount: 5, isActive: true },
];

export const mockPackets: Packet[] = [
  {
    id: 'pkt-001',
    timestamp: Date.now() - 1000,
    sourceIp: '192.168.1.100',
    sourcePort: 54321,
    destIp: '192.168.1.1',
    destPort: 80,
    protocol: 'TCP',
    size: 1460,
    layer: 'PORT',
    dropped: false,
  },
  {
    id: 'pkt-002',
    timestamp: Date.now() - 2000,
    sourceIp: '10.0.0.50',
    sourcePort: 12345,
    destIp: '192.168.1.1',
    destPort: 443,
    protocol: 'TCP',
    size: 512,
    layer: 'PORT',
    dropped: false,
  },
  {
    id: 'pkt-003',
    timestamp: Date.now() - 3000,
    sourceIp: '172.16.0.10',
    sourcePort: 33333,
    destIp: '192.168.1.1',
    destPort: 22,
    protocol: 'TCP',
    size: 64,
    layer: 'FW',
    dropped: true,
    dropReason: 'FIREWALL_RULE',
  },
  {
    id: 'pkt-004',
    timestamp: Date.now() - 4000,
    sourceIp: '8.8.8.8',
    sourcePort: 0,
    destIp: '192.168.1.1',
    destPort: 0,
    protocol: 'ICMP',
    size: 84,
    layer: 'PORT',
    dropped: false,
  },
  {
    id: 'pkt-005',
    timestamp: Date.now() - 5000,
    sourceIp: '192.168.1.200',
    sourcePort: 60000,
    destIp: '192.168.1.1',
    destPort: 3306,
    protocol: 'TCP',
    size: 256,
    layer: 'FW',
    dropped: true,
    dropReason: 'RATE_LIMIT',
  },
  {
    id: 'pkt-006',
    timestamp: Date.now() - 6000,
    sourceIp: '10.10.10.10',
    sourcePort: 11111,
    destIp: '192.168.1.1',
    destPort: 8080,
    protocol: 'UDP',
    size: 1024,
    layer: 'NIC',
    dropped: true,
    dropReason: 'XDP_DROP',
  },
  {
    id: 'pkt-007',
    timestamp: Date.now() - 7000,
    sourceIp: '203.0.113.1',
    sourcePort: 45678,
    destIp: '192.168.1.1',
    destPort: 80,
    protocol: 'TCP',
    size: 0,
    layer: 'NIC',
    dropped: true,
    dropReason: 'INVALID_PACKET',
  },
];

// Helper functions to filter packets by layer
export const getPortLayerPackets = () => mockPackets.filter((p) => p.layer === 'PORT');
export const getFWLayerPackets = () => mockPackets.filter((p) => p.layer === 'FW');
export const getNICLayerPackets = () => mockPackets.filter((p) => p.layer === 'NIC');
